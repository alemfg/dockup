fix(v4.0.4): FleetHealth stat boxes layout — all 6 in one uniform grid row

Adding Paused and Blocked boxes created two separate grids: 5 boxes on
the first row, Dead alone on a second row below it.

Fix: single grid with grid-cols-3 (mobile) / grid-cols-6 (sm+).
All six boxes — Total, Healthy, Degraded, Paused, Blocked, Dead — sit
in one consistent row on wider screens and wrap neatly 3×2 on mobile.

FILES CHANGED
-------------
frontend/src/components/FleetHealth.jsx

VERSION
-------
4.0.4
